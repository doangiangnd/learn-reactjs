import React from 'react';
import PropTypes from 'prop-types';
import './style.scss'

Header.propTypes = {

};

function Header(props) {
    return (
        <div>
            <header></header>
        </div>
    );
}

export default Header;